{
  "name": "map-char",
  "version": "1.0.5",
  "description": "The mapChar() function iterates over each character of the string and executes the provided callback function for each one.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "GkChris",
  "license": "ISC",
  "keywords": [
    "map",
    "string",
    "loop",
    "char",
    "iretate"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:GkChris/map-char.git"
  }
}
